from distutils.core import setup

setup(name='pysv', version='0.3.0', packages=['pysv'])

# Run the line below to install the package:
# python3 -m pip install -e <absolute path to the directory containing this path>
